services:
  autojudge:
    build: .
    ports:
      - "9000:9000"
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - ./data:/app/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  # Python sandbox for code execution verification
  sandbox:
    image: python:3.11-slim
    command: ["sleep", "infinity"]
    network_mode: none
    read_only: true
    tmpfs:
      - /tmp:size=64m
    deploy:
      resources:
        limits:
          memory: 256m
          cpus: "0.5"

volumes:
  redis_data:
